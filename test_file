open "fmt"
let mutable text = 1

type TestRecord = {
	TestVal: []*int
	mutable AnotherTest: string
}

type TestADT =
	| Test
	| Testing(string)

let getValue (&t: *TestRecord) = t.testVal

let main () = (
	let record = {
		TestVal: 1,
		AnotherTest: "Hello World"
	}

	|> fn (x) {
		x.AnotherTest = "Hello Again"
		x
	}
	|> fmt.Println
	()
)

let test testValue = (
	testValue.TestVal
)

